export default function AdminPage() {
  return (
    <main className="container">
      <section className="section">
        <h2>Admin (starter)</h2>
        <p className="muted">
          In v1, use Supabase dashboard tables directly for events, gifts, and RSVPs. Next step is adding a protected
          admin UI.
        </p>
      </section>
    </main>
  );
}
